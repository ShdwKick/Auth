# Аккаунты, сессии и ПРИВАТНЫЕ КЛЮЧИ ПОДПИСИ — никогда не коммитить
data/

# Секреты для docker-compose.prod.yml (ADMIN_INTERNAL_KEY и т.п.) — подставляются
# через ${VAR:-}, живут только на сервере, права 600
.env

# Временные базы, которые создают себе проверки из test/
test/.work*

# Артефакты ОС/редакторов
*.log
.DS_Store
Thumbs.db
